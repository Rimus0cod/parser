# ---------------------------------------------------------------------------
# Core App
# ---------------------------------------------------------------------------
APP_NAME=Real Estate SaaS Core
APP_ENV=prod
APP_DEBUG=false

# ---------------------------------------------------------------------------
# MySQL
# ---------------------------------------------------------------------------
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=app_user
MYSQL_PASSWORD=change_me_mysql_password
MYSQL_DATABASE=imoti
MYSQL_ROOT_PASSWORD=change_me_root_password

# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# ---------------------------------------------------------------------------
# Scraper Runtime
# ---------------------------------------------------------------------------
SCRAPE_BASE_URL=https://imoti.bg/наеми/page:{page}
SCRAPE_MAX_PAGES=10
SCRAPE_TIMEOUT_SECONDS=30
SCRAPE_INTERVAL_SECONDS=900
SCRAPE_CONCURRENCY=8
CITY_FILTER=

# ---------------------------------------------------------------------------
# Streamlit Auth (streamlit-authenticator cookie/JWT style)
# ---------------------------------------------------------------------------
STREAMLIT_COOKIE_NAME=lead_scanner_cookie
STREAMLIT_COOKIE_KEY=change_me_cookie_key_please
STREAMLIT_COOKIE_EXPIRY_DAYS=7
STREAMLIT_JWT_SECRET=change_me_jwt_secret_32chars_min
STREAMLIT_USERS_YAML_PATH=app/ui/users.yaml

# ---------------------------------------------------------------------------
# Legacy compatibility vars (optional; keep if old scripts still used)
# ---------------------------------------------------------------------------
GOOGLE_SHEET_ID=
SERVICE_ACCOUNT_JSON=./google.json
SHEET_NAME=Imoti_BG_Rentals
EMAIL_FROM=
EMAIL_TO=
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAX_PAGES=26
REQUEST_DELAY_MIN=2.0
REQUEST_DELAY_MAX=5.0
LOG_LEVEL=INFO
LOG_FILE=./parser.log
AGENCIES_CSV_PATH=./agencies.csv
MYSQL_ENABLED=true
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
